# Exact versions used to produce the bundled figures, tables and logs
# (Python 3.10.12; matplotlib 3.10.9 is also recorded in the PNG metadata of
# every script-generated figure). Pinned so the archived snapshot reproduces
# under the environment that made it. Any later release satisfying the floors
# in the comment beside each line is expected to work but has not been tested.
numpy==1.26.4        # >=1.26
pandas==2.2.3        # >=2.2
matplotlib==3.10.9   # >=3.10
seaborn==0.13.2      # >=0.13
scipy==1.15.2        # >=1.15
scikit-learn==1.6.1  # >=1.6
joblib==1.4.2        # >=1.3
statsmodels==0.14.6  # >=0.14
Pillow==11.1.0       # >=10.0
